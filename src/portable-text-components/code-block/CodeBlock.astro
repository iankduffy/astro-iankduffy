---
import { Code } from 'astro:components';
import CSS from './styles.module.scss'
const { node } = Astro.props
const { code, language = 'ts' } = node
---


<Code code={code} class:list={[CSS.codeBlock]} lang={language} />
